{% extends 'layout_admin.html.twig' %}

{% block title %}Actualités{% endblock %}

{% block body %}

    <div class="container-content">

        <h1 class="title">Actualités</h1>

        {% include '_backToTop.html.twig' %}

        <div class="container-fluid admin-container admin-articles">

            <div class="mt-n5 mb-5 d-flex justify-content-start">
                <a href="{{ path('article_new') }}">
                    <button class="btn btn-primary"><i class="fas fa-plus"></i> Ajouter un article</button>
                </a>
                <a class="ml-5" href="{{ path('section_index') }}">
                    <button class="btn btn-secondary"><i class="fas fa-share mr-2"></i> Se rendre aux sections</button>
                </a>
            </div>

            <table class="table">
                <thead>
                <tr>
                    <th>Id</th>
                    <th>Photo</th>
                    <th>Section</th>
                    <th>Titre</th>
                    <th>Contenu</th>
                    <th>Date</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for article in articles %}
                    <tr>
                        <td>{{ article.id }}</td>
                        <td> {% if article.filename %}
                                <img src="{{ vich_uploader_asset(article, 'pictureFile') }}" alt="image article"
                                     class="img-article">
                            {% endif %}</td>
                        <td><span style="color:{{ article.section.color ?? '' }}">{{ article.section.name ??'' }}</span>
                        </td>
                        <td>{{ article.title }}</td>
                        <td>{{ article.content|striptags|truncate(200) }}</td>
                        <td>{{ article.date ? article.date|date('d/m/Y H:i') : '' }}</td>
                        <td class="buttons">
                            <a href="{{ path('article_edit', {'id': article.id}) }}" class="btn btn-primary mb-2 mt-4">
                                <i class="fas fa-pen mr-2"></i> Modifier
                            </a>
                            {% include 'admin_article/_delete_form.html.twig' %}
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="6">no records found</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>

    </div>

{% endblock %}
