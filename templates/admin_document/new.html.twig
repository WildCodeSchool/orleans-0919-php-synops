{% extends 'layout_admin.html.twig' %}

{% block title %}Ajouter un outil{% endblock %}

{% block body %}
    <div class="container-content">
        <h1 class="title">Ajouter un outil</h1>

        <div class="container mb-5">
            <a href="{{ path('tool_index') }}"> <button class="btn btn-secondary"><i class="fas fa-reply mr-2"></i> Retour aux outils</button></a>
        </div>

        <div class="container">
            {{ include('admin_document/_form.html.twig') }}
        </div>
    </div>
{% endblock %}
