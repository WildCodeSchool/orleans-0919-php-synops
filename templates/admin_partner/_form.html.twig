{{ form_start(form) }}
{{ form_row(form.partnerFile) }}
    {{ form_widget(form) }}
<div class="d-flex justify-content-center mt-5">
    <button class="btn btn-primary"><i class="fas fa-save mr-2"></i> {{ button_label|default('Sauvegarder') }}</button>
</div>
{{ form_end(form) }}
