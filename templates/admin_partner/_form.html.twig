{{ form_start(form) }}
{{ form_row(form.partnerFile) }}
    {{ form_widget(form) }}
<button class="btn btn-primary"><i class="fas fa-save"></i> {{ button_label|default('Sauvegarder') }}</button>
{{ form_end(form) }}
