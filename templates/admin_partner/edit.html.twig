{% extends 'layout_admin.html.twig' %}

{% block title %}Modifier un partenaire{% endblock %}

{% block body %}
    <div class="container-content admin-container">
        <h1 class="title">Modifier {{ partner.name }}</h1>

        <div class="container mb-5">
            <a href="{{ path('partner_index') }}">
                <button class="btn btn-secondary"><i class="fas fa-reply"></i> Tous nos partenaires</button>
            </a>
        </div>

        <div class="container">
            {{ include('admin_partner/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            {{ include('admin_partner/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
