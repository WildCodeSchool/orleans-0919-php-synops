{{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_row(form.firstname) }}
    {{ form_row(form.lastname) }}
    {{ form_row(form.phone) }}
    {{ form_row(form.email) }}
    {{ form_row(form.linkedin) }}
    {{ form_row(form.description) }}
    {{ form_row(form.imageFile) }}
{{ form_rest(form) }}
<div class="d-flex justify-content-center mt-5">
    <button class="btn btn-primary mb-5"><i class="fas fa-save mr-2"></i> {{ button_label|default('Sauvegarder') }}</button>
</div>
{{ form_end(form) }}
