{% extends 'layout_admin.html.twig' %}

{% block title %}Modifier une sous-catégorie{% endblock %}

{% block body %}
    <div class="container-content">
        <h1 class="title">Modifier une sous-catégorie</h1>

        <div class="container mb-5">
            <a href="{{ path('tool_index') }}">
                <button class="btn btn-secondary"><i class="fas fa-reply mr-2"></i> Retour aux outils</button>
            </a>
        </div>

        <div class="container adminUpdateButton">
            {{ include('admin_tool/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>
{% endblock %}
